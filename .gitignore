# Dependencies
node_modules/

# Build output
dist/
out/
# electron-builder 复用 build/ 作图标等源资源输入（约定目录名），不是生成产物；
# 用 build/* 而不是 build/，否则父目录整个被排除后 git 不会再进去看 negation pattern
build/*
!build/icon.png

# Electron
release/

# Environment & secrets
.env
.env.local
config.json

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Logs
*.log
npm-debug.log*

# Run summary 文件（Plan 7：每次 skill pipeline 跑完写，不入库）
logs/

# Test coverage
coverage/
.nyc_output/

# Internal docs (design/competition/plans — 参赛期间不公开)
docs/

# Plan 8 spike 临时产物
scripts/.pi-spike-work/
